<h1>Users</h1>

<ul>
<% @users.each do |user| %>

	<%= link_to( user.username, user_path(user.id)) %>
	<li><%= user.name %></li>
	<li><%= user.email %></li>
	<li><%= user.password_digest %></li>

<% end %>
</ul>
