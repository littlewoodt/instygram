<h2>instantgramme</h2>

<div class="posts">

<% @posts.each do |post| %>
	<div class="post">
		<h4><%= post.title %></h4>
		<%= link_to(image_tag(post.image, :class => 'thumb'), post) %>
		<p><b><%= post.user.name if post.user %></b> <%= post.content %></p>
		
	</div>
<% end %>

</div>
	
