<h1>instantgramme</h1>

<% @posts.each do |post| %>
	<%= link_to(image_tag(post.image, :class => 'thumb'), post) %>
	<p><%= post.title %></p>
	<p><%= post.content %></p>
	<p><%= post.user.name if post.user %></p>
<% end %>
	